apiVersion: v1
kind: Service
metadata:
  name: ui-dashboard-service
  namespace: i3m-platform
  labels:
    app: ui-dashboard
    component: frontend
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
    name: http
  selector:
    app: ui-dashboard
